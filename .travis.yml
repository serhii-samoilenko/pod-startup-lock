sudo: required

language: go
go:
  - "1.10"

go_import_path: ./

services:
  - docker

install:
  - mkdir -p "${GOPATH}/bin"
  - curl https://glide.sh/get | sh

script: make -C src glide linux build docker-build docker-push